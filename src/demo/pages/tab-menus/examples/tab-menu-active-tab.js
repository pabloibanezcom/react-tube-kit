const html = `<TabMenu
  color="secondary"
  tabs={mockTabs}
  activeTab={2}
/>`;

const tabMenuActiveTab = {
  id: 'activeTab',
  name: 'With initial active tab',
  html
};

export default tabMenuActiveTab;
