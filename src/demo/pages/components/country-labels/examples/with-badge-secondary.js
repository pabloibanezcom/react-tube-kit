const html = `<CountryLabel
  badge
  type="secondary"
  country={{code: 'GB', name: 'United Kingdom'}}
/>`;

const withBadgeSecondary = {
  id: 'withBadgeSecondary',
  name: 'With Badge Secondary',
  html
};

export default withBadgeSecondary;
