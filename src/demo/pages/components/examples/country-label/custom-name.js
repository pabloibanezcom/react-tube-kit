const html = `<CountryLabel
  country={{code: 'GB', name: 'United Kingdom'}}
  customName="London"
/>`;

const customName = {
  id: 'customName',
  name: 'Custom name',
  html
};

export default customName;
