const html = `<ImageAction
  src={imgSample}
  actionText="Do something"
  onClick={handleOnClick}
/>`;

const imageActionBasic = {
  id: 'imageActionBasic',
  name: 'Basic',
  html
};

export default imageActionBasic;
