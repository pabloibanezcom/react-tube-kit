const html = `<div className="sample-div" >
  <LoadingSpinner
    loading={isLoading}
  />
</div>`;

const loadingSpinnerPrimary = {
  id: 'primary',
  name: 'Primary',
  html
};

export default loadingSpinnerPrimary;
