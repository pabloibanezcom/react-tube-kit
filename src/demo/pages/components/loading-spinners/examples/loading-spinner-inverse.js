const html = `<div className="sample-div" >
  <LoadingSpinner
    inverse
    loading={isLoading}
  />
</div>`;

const loadingSpinnerInverse = {
  id: 'inverse',
  name: 'Inverse',
  html
};

export default loadingSpinnerInverse;
